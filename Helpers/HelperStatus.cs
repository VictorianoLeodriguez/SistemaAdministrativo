public enum CargoSystem{
    Admin = 0,
    Client = 1,
}

public enum CargoStatusClient
{
    Ativo = 0, 
    inativo = 1
}

public enum CargoInterno
{
    RH = 0,
    Contabilidade = 1,
    Administrativo = 2,
    Fiscal = 3
}